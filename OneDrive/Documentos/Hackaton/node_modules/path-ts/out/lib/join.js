/**
 * @copyright Sister Software
 * @license AGPL-3.0
 * @author Teffen Ellis, et al.
 */
import { join as _join } from "node:path";
/**
 * Join all arguments together and normalize the resulting path.
 *
 * @throws {TypeError} If path is not a string or {@linkcode PathBuilder}.
 */
export function join(pathBuilderLike, ...pathSegmentN) {
    const joinedPath = _join(
    // ---
    pathBuilderLike.toString(), ...pathSegmentN.map((pathSegment) => pathSegment.toString()));
    return joinedPath;
}
export default join;
//# sourceMappingURL=join.js.map